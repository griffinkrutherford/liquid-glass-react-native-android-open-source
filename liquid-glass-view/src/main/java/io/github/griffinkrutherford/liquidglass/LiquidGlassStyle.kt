package io.github.griffinkrutherford.liquidglass

/** Material variants aligned with the observable modes exposed by Callstack's iOS library. */
enum class LiquidGlassEffect {
    CLEAR,
    REGULAR,
    NONE,
}

enum class LiquidGlassColorScheme {
    LIGHT,
    DARK,
    SYSTEM,
}
